<UserControl x:Class="FinalProject_8327_4647.UserControls.ImageSearchUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:FinalProject_8327_4647.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewModel:ImageSearchVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <viewModel:ImageSearchVM x:Key="searchVM"/>
    </UserControl.Resources>
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft">
            <Button Name="searchBT" Command="{Binding SearchCMD}" Content="חיפוש" Margin="3"/>
            <TextBox Name="searchTB" Width="300" Text="{Binding SearchVal}" />

        </StackPanel>

        <ListView ItemsSource="{Binding SearchImageCollection}" HorizontalAlignment="Stretch" Height="900">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4" HorizontalAlignment="Stretch"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                        <Image Source="{Binding Url}" Width="200" Height="200" Stretch="Uniform" />
                        <TextBlock Text="{Binding Title}" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!--ListBox x:Name="itemsList" ItemsSource="{Binding SearchImageCollection}" Height="300" Margin="30" IsSynchronizedWithCurrentItem="True">            <ListBox.ItemTemplate>                <DataTemplate>                    <StackPanel Orientation="Horizontal">                        <TextBox x:Name="resTitle" Text="{Binding Title}" Width="300"/>                        <Image Source="{Binding Url}" Height="100"/>                    </StackPanel>                </DataTemplate>            </ListBox.ItemTemplate>        </ListBox>-->
    </StackPanel>
</UserControl>
